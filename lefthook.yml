pre-commit:
  parallel: true
  commands:
    rubocop:
      glob: "{Gemfile,Rakefile,config.ru,**/*.rb,**/*.rake,bin/*}"
      run: bundle exec rubocop --force-exclusion {staged_files}

pre-push:
  commands:
    rspec:
      run: bundle exec rspec
